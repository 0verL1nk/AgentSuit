# minimal-starter

Minimal example fixture for validating the AgentSuit workspace scaffold.

It contains a single prompt overlay and a compact `suit.yaml` manifest so future `validate`, `pack`, and `apply` work has a stable sample target in the repository.
