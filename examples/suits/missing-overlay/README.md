# missing-overlay

Invalid example fixture for validation failure scenarios.

The manifest references a prompt overlay file that is intentionally absent so
CLI and core validation tests can assert deterministic `FILE_MISSING` results.
